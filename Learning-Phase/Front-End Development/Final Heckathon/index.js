// Find Unique Elements

// Write a function named findUnique that takes an array of integers and returns an array of elements that appear only once in the input array.

// findUnique([4, 5, 4, 6, 7, 7, 8]);  // [5, 6, 8]   

// findUnique([1, 2, 3, 2, 1, 4]);  // [3, 4]

let unique = [1, 2, 3, 4, 5, 2, 4, 1, 1];
    
        function findUnique(arr) {
            // var obj = {};

            // for (var i = 0; i < arr.length; i++) {
            //     var num = arr[i];
            //     if (obj[num] === undefined) {
            //         obj[num] = 1;
            //     } else {
            //         obj[num]++;
            //     }
            // }

            // var unique = [];
            // for (var key in obj) {
            //     if (obj[key] === 1) {
            //         unique.push(Number(key));
            //     }
            // }

            return unique;
        }

    